# Changelog

## 0.3.9 — 2026-09-25

- 在 v0.3.8 上加入抗干扰跟踪：卡尔曼预测加对齐后的帧差。掩膜跳到条纹或树叶上时标为不可见，不写进记忆，也不带到下一窗口。
- SAM 没给出掩膜时，先在预测位置附近用运动团块把球找回来。
- 仍找不到、且缺口不超过 0.12 秒时，用两侧轨迹做二次拟合补点，并标为插值。更长的缺口保持不可见。
- 菜单「轨迹 → 抗干扰（背景相减 + 运动预测）」默认开启。被拒收的点在数据表里标黄，提示「已拒收：疑似跳到背景」。

## 0.3.3 — 2026-09-22

- 首次打开的过场教程改成卡片内自绘动画（抛物线跟踪、框选、快速/精准等），真实界面仍可操作但不会自动翻页；跟踪拆成 6 步，并写清框是目标提示不是搜索范围。
- 应用内更新先把新版本拷到旁边，再备份旧版；替换失败会把旧版移回去，并提示已恢复。
- 每个 Release 附带 `update.json`。低于最低版本，或这次标记为关键补丁时，不能跳过。
- 第一次从 DMG 安装若被系统拦住，右键「打开」一次即可。之后的应用内更新会自动去掉隔离标记。
- 快速模式不再把画面缩到 512 再放大回 1024；改为原生分辨率隔帧跟踪，推理开销不变但掩膜更准。
- 隔帧之间的补点改用 Catmull-Rom：抛物线轨迹不再「塌腰」，遮挡两侧仍然不连线。
- SAM 帧张量改为按需解码，不再把整段视频堆进内存；长视频按窗口跟踪，内存不随时长增长。
- 采样帧较多时自动把推理状态放到 CPU，8GB 机器跑长片不再爆内存。
- macOS 安装包剔除用不到的 Qt WebEngine / Quick3D / Designer、Qt 自带的孤儿 ffmpeg 与 torch C++ 头文件，DMG 从 571 MB 降到 400 MB，安装后占用从 1.2 GB 降到 840 MB。
- 构建脚本在签名前跑一次 `--smoke`，瘦身瘦过头会直接构建失败而不是发到学生手里才崩。

## 0.3.2 — 2026-09-07

- 首次打开时逐步高亮主界面（导入视频、跟踪、标定、分图、数据表、助手、播放），可跳过；帮助菜单「快速开始」可再看一遍。
- 高亮区域可点、可拖入；导入成功后自动下一步，其它步点到控件后继续。
- 演示指针会在真实画面上框选、划尺、拖分图，并按下按钮、点亮拖入区；不是只画在遮罩上。
- 背景补偿改为保守的分段估计：分析尺度下采样、平移优先、排除运动主体，并用抖动门控禁止把数据打乱；失败片段逐点保持原坐标。
- 瞬时速度改为按真实 PTS 的稳健局部多项式求导；Tracker 中心差分仍可选。斜抛段叠加独立的模型速度虚线，不覆盖测量点。

## 0.3.1 — 2026-09-06

- macOS 安装包可在应用内点「立即更新」：下载对应架构的 DMG、校验 SHA-256、替换当前 `.app` 并重启。
- 从源码运行，或安装包目录不可写时，仍打开 GitHub Releases 下载页。

## 0.3.0 — 2026-09-06

- 跟踪为快速 Tiny（隔帧插值）与精准 Small（逐帧）；缺权重时右下角显示下载进度。
- 帮助菜单可检查 GitHub Releases；启动约 2.5 秒后异步检查，已装 0.1.0 / 0.2.0 的用户可升到本版本。
- 手工平面测量（单应）与近远尺、单尺并存；可选 ChArUco 与离面抽检。
- 分图可拖动 scrub，轴用整数等差刻度，当前帧点放大。
- macOS 标准安装包（arm64 / x86_64）捆绑 Tiny；精准 Small 首次使用时下载。

## 0.2.0 — 2026-09-06

- macOS 安装包包含 SAM 2.1 Tiny 官方权重。
- 标定尺长度改为白字描边，标定窗口文字改为浅色。
- AI 离面抽检仍为可选，未打入安装包。

## 0.1.0 — 2026-09-05

- 首次 macOS 标准版 DMG（arm64 / x86_64）。
- 启动异步检查 GitHub Releases；帮助菜单提供检查更新、自动检查与关于。
- 手工平面测量（单应）与近远尺、单尺并存。
