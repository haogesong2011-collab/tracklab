# Optional SAM 2.1 Tiny runtime for the desktop tracker.
# Not required for `python -m tests.ai.ci`.
#
#   .venv/bin/python -m pip install -r requirements-ai.txt
#
# Weights are downloaded on first use into ~/.cache/tracklab/models/
# (Apache 2.0, facebook/sam2.1-hiera-tiny).
# Video frames are decoded with PyAV; decord is not required.
#
# Optional MoGe-2 ViT-S for sparse off-plane audit (not used by fast tracking):
#   python -m pip install moge-2

torch>=2.5.1
torchvision>=0.20.1
hydra-core>=1.3.2
iopath>=0.1.10
certifi>=2024.0.0
sam-2 @ git+https://github.com/facebookresearch/sam2.git
