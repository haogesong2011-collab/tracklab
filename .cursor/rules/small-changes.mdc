---
description: 小改动单独分支推到 GitHub，方便另一位开发者直接拉取运行
alwaysApply: true
---

# 小改动的交付

每一项小改动单独处理，不要和无关的未提交改动打在同一次提交里。

1. 从当前 GitHub 分支拉出新分支，只包含这一项改动。
2. 提交、推送到 origin，并开 Pull Request。
3. 对方拉取该分支后，用项目里的虚拟环境运行即可装上（`./run.sh` 或 `.venv/bin/python -m app`）。
4. 本机工作区里其他尚未提交的改动留在原地，不进这个分支。
